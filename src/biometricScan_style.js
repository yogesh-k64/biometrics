import { StyleSheet } from "react-native";

export const BiometricScan_styles = StyleSheet.create({
    container:{
        flex:1,
        justifyContent:'center',
        alignItems:'center',
    },
    webView:{
        flex:1
    }
})